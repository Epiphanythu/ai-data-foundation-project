# common 共享工具模块

`common/` 放置跨模块复用的工具函数，避免在多个业务模块中复制代码。

## 文件说明

| 文件 | 作用 |
|---|---|
| `model_data.py` | 统一构造建模样本，封装 Lending Club 字段清洗、标签构造和特征选择 |
| `llm_client.py` | 统一封装 LLM 客户端，读取 `constant/llm.py` 中的配置 |

## 使用原则

- 多个模块都会用到的逻辑放入 `common/`。
- 业务专属逻辑留在对应业务模块中。
- `common/` 不直接承担完整业务流程入口。

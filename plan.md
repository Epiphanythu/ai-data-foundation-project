# 项目执行路线

> 清华大学 AI 数据基础课程大作业 · 个人贷款违约风险检测
> 两人组：xwp（数据 / 建模 / 工程 / 智能交互）+ zyf（时序 / 因果 / 动态决策）

---

## 一、项目定位

中期评分 14/20。后续阶段不再按“补功能”推进，而是围绕一个更强主线收束：

> **面向宏观非平稳环境的个人贷款违约动态风险决策系统**

### 1.1 核心判断

项目的核心价值不是单纯提升 AUC，也不是堆叠 Dashboard 页面，而是回答一个更贴近真实金融风控的问题：

> 当宏观经济环境发生变化时，贷款违约风险模型是否应该感知环境状态，并进一步调整审批阈值、解释拒绝原因和追溯决策链路？

### 1.2 推进原则

- 方法创新和量化验证必须合在一起，避免只做分析而没有收益结论。
- AutoML 必须服务状态感知建模，避免退化成普通调参流程。
- Dashboard 必须展示核心结论，避免扩成静态图册或页面堆叠。
- LLM 助手必须查询和解释核心产物，避免只是独立工具。

---

## 二、阶段目标

老师反馈要求 T1/T2/T3，但项目不能平均用力。新的目标是：

| 目标 | 新定位 | 对应模块 |
|---|---|---|
| T1 交互式可视化系统 | 展示 M1 的核心结论，而不是扩成静态图册 | M3 |
| T2 自然语言接口 + LLM 自动可视化 | 已完成，作为 M1 结果查询入口 | M2 |
| T3 数据驱动 AutoML | 不做泛泛 AutoML，改成“状态感知建模验证” | M1 |

核心交付从“多个功能增强”改成：

> **状态识别 → 状态感知建模 → 动态阈值优化 → 反事实解释 → 决策追溯 → Dashboard / LLM 展示**

---

## 三、任务总览

任务划分采用“核心方法 + 智能助手 + 展示系统 + 交付收口”的结构，确保所有工作围绕动态风险决策主线展开。

| 模块 | 定位 | 优先级 | 状态 |
|---|---|---|---|
| **M1 核心方法：状态感知动态风控建模与策略优化** | 研究创新 + 量化验证 | P0 | 待集中推进 |
| **M2 LLM 智能分析助手** | 自然语言查询、自动报告、自动出图 | P1 | ✅ 已完成并推送 |
| **M3 Dashboard 核心展示系统** | 展示 M1/M2 的关键结论 | P1 | 待重组 |
| **M4 报告 / Demo / 答辩收口** | 最终表达和交付 | P1 | 待推进 |

关键原则：

- **M1 是研究内核**：后续主要时间投入到 M1，确保有方法、有实验、有结论。
- **M2 不再大改**：已完成的 LLM 能力只做稳定性维护，后续接 M1 产物。
- **M3 不追求 Tab 数量**：只围绕状态风险、模型验证、动态策略、决策追溯做展示。
- **M4 提前准备**：报告和 demo 要围绕 M1 讲，不按代码模块流水账。

---

## 四、M1 核心方法：状态感知动态风控建模与策略优化

### 4.1 M1 的研究问题

M1 围绕一个完整问题展开：

> 在宏观经济环境非平稳变化时，个人贷款违约风险模型是否应该感知宏观状态，并据此调整审批阈值和解释决策？

对应 4 个可验证假设：

| 假设 | 含义 | 验证指标 |
|---|---|---|
| H1 状态差异 | 不同宏观状态下违约率显著不同 | 状态分组违约率、趋势图、差异检验 |
| H2 建模增益 | 加入状态/时序/宏观特征后模型表现更好 | AUC、KS、PR-AUC、Calibration |
| H3 策略增益 | 动态阈值比固定阈值更适合非平稳环境 | 利润、坏账率、通过率、坏账拦截率 |
| H4 解释增益 | 反事实和追溯能给出可执行的风险解释 | 个案链路、最小改善路径、拒绝原因 |

### 4.2 M1 子任务

| 子任务 | 内容 | 产物 | 验收标准 | 负责人 |
|---|---|---|---|---|
| **M1-1 宏观状态构造** | 基于 FRED / ERS / 季度变量构造正常期、压力期、高利率期等状态 | `macro_state_features.csv`、状态分布图、状态违约率对比图 | 至少 2-3 个状态，且违约率/业务指标有可解释差异 | zyf 主，xwp 协 |
| **M1-2 状态感知特征工程** | 将宏观状态、时序滞后、州级经济变量与贷款特征统一进建模数据 | 特征清单、缺失率表、建模数据构造函数 | 保持 baseline 向后兼容，不污染默认特征空间 | zyf + xwp |
| **M1-3 状态感知建模验证** | 比较 baseline、宏观特征、时序特征、状态特征组合 | 模型指标表、AUC/KS 对比图、Top 特征表 | 至少给出“提升/不提升”的量化结论 | xwp 主 |
| **M1-4 动态阈值策略优化** | 比较固定阈值与分状态阈值，优化利润/坏账/通过率 | 最佳阈值表、利润曲线、策略对比表 | 明确动态策略在什么状态下优于固定策略 | xwp 主，zyf 协 |
| **M1-5 反事实与决策追溯** | 选典型样本展示评分、状态、阈值、审批、解释、改善路径 | 反事实案例表、决策追溯表、案例图 | 至少 3 个样本能讲清完整审批链路 | zyf 主，xwp 协 |

### 4.3 xwp 部分如何加深

xwp 的任务不再只是“训练模型 + 做 Dashboard”，而要形成方法验证闭环：

| xwp 任务 | 深化方向 | 必须输出 |
|---|---|---|
| 数据融合 | 增加状态标签、时序滞后、状态-客群交互特征的数据质量检查 | 特征覆盖率、缺失率、状态分布 |
| 建模验证 | 做特征组消融：Baseline / +Macro / +Temporal / +State / +All | AUC、KS、PR-AUC、Calibration 对比 |
| AutoML | 状态感知 AutoML：验证状态/时序/宏观特征是否被模型选择、是否提升业务指标 | best params、Top 特征、消融结论 |
| 风控策略 | 固定阈值 vs 状态阈值 vs 分客群阈值，比较利润和坏账 | 阈值收益表、策略提升百分比 |
| 可解释性 | SHAP + 反事实 + 决策链路结合，解释“为什么拒绝、怎样变成可通过” | 个案解释卡片 |
| 工程集成 | 把 M1 关键产物整理成 Dashboard 和 LLM 可查询的数据源 | 标准 CSV / PNG / markdown 产物 |

### 4.4 M1 最小可交付版本

如果时间紧，M1 不做大而全，优先保证下面 4 个结论：

1. **状态确实不同**：不同宏观状态下违约率、坏账率或最优阈值有明显差异。
2. **状态特征有价值**：加入宏观/时序/状态特征后，模型指标或业务指标有量化变化。
3. **动态阈值有业务意义**：至少在压力期或高风险状态下，动态阈值能降低坏账或提高利润。
4. **个体决策可追溯**：至少展示 3 个样本的“特征 → 分数 → 状态 → 阈值 → 决策 → 解释”。

---

## 五、M2 LLM 智能分析助手

M2 不只是“自然语言问答 + 自动画图”，而是面向金融风险分析场景的 **LLM 分析代理**。它的价值在于把 M1 的状态感知建模、动态阈值和决策追溯结果转化为可查询、可解释、可复现的分析过程。

### 5.1 核心定位

> 让评委可以用自然语言追问 M1 的核心结论，并得到“数据证据 + 图表 + 解释 + 生成代码”的完整回答。

M2 要回答的不是普通统计问题，而是围绕风险决策的深层问题：

| 问题类型 | 示例问题 | 期望输出 |
|---|---|---|
| 状态风险解释 | 哪个宏观状态下违约率最高？为什么？ | 状态违约率表、趋势图、中文解释 |
| 模型增益解释 | 状态感知模型相比 baseline 提升在哪里？ | AUC/KS 对比、Top 特征、提升幅度 |
| 策略收益解释 | 动态阈值相比固定阈值提升多少利润？ | 策略收益表、利润曲线、坏账率变化 |
| 决策追溯解释 | 这笔贷款为什么被拒？怎样才能通过？ | 审批链路、关键因素、反事实改善建议 |
| 稳健性追问 | 压力期和正常期的最优阈值是否不同？ | 分状态阈值对比、差异说明 |

### 5.2 分层能力设计

| 层级 | 能力 | 深化要求 | 产物 |
|---|---|---|---|
| L1 语义路由 | 根据问题自动匹配数据源 | 从固定数据源选择升级为“问题意图 → 数据源 → 分析模板” | 数据源注册表、预设问题集 |
| L2 安全执行 | 生成并执行受控 pandas/numpy 代码 | AST 白名单、黑名单、最多 3 次重试、禁止 IO/网络/plt/exec/eval | 生成代码、执行结果、错误重试记录 |
| L3 自动可视化 | 根据问题选择图表类型 | 趋势→折线，对比→柱状，分布→直方，二维关系→散点/热力图 | `chart`、`chart_title`、`chart_note`、PNG 下载 |
| L4 证据化解释 | 回答必须绑定数据证据 | 不只给结论，还要给指标、排序、提升幅度或阈值变化 | answer 表格、中文解释、图表说明 |
| L5 追问式分析 | 支持围绕同一问题继续追问 | 多轮历史保留问题、数据源、代码、图表和结果 | 历史记录、代码折叠、案例复盘 |

### 5.3 已完成能力

✅ 已完成并推送，提交：`1447f80 feat: enhance multimodal llm assistant`。

| 能力 | 内容 |
|---|---|
| 配置与客户端 | `.env` 配置加载、缺失配置明确报错、兼容 `glm-5.1 reasoning_content` |
| 自动报告 | 读取核心产物生成 Markdown 报告，报告 `max_tokens=6000` |
| 多数据源问答 | 支持州级、季度、分层风险、模型指标、风控策略等数据源 |
| 受控代码生成 | LLM 输出 `answer`，并可输出 `chart_title` / `chart_note` / `chart` |
| 安全沙箱 | 禁止 import / 文件 IO / 网络访问 / plt / exec / eval，通过 AST 白名单执行 |
| 自动出图 | 支持 `plot_bar` / `plot_line` / `plot_hist` / `plot_scatter` / `plot_heatmap` |
| Dashboard 集成 | 数据源选择、预设问题、多轮历史、代码折叠、图表解释、PNG 下载 |
| 运行时隔离 | `outputs/figures/llm_charts/` 已加入 `.gitignore`，避免误提交 |

### 5.4 继续加深方向

后续 M2 的增强重点不是增加更多花哨交互，而是让它成为 M1 的“智能审稿人”和“现场分析员”。

| 加深方向 | 具体任务 | 验收标准 |
|---|---|---|
| M1 数据源接入 | 接入宏观状态表、模型消融表、动态阈值表、决策追溯案例表 | AI 助手能回答 M1 四类核心问题 |
| 证据约束回答 | Prompt 要求回答必须包含指标值、排序或提升幅度 | 回答不能只有泛泛解释，必须引用数据结果 |
| 分析模板化 | 为状态风险、模型对比、策略收益、追溯案例设计默认分析模板 | 预设问题一键生成稳定图表和解释 |
| 结果一致性校验 | 对空结果、缺列、无图表、格式不合规进行重试或友好提示 | 失败时不崩溃，能说明缺失原因 |
| 报告自动升级 | 自动报告从通用总结升级为“M1 核心结论报告” | 报告包含状态差异、模型增益、策略收益、追溯案例 |
| Demo 问题库 | 准备 6-8 个现场稳定问题，覆盖 M1/M2/M3 | 现场可连续演示，不依赖临时发挥 |

### 5.5 M2 验收标准

- 至少支持 6 类数据源：状态风险、模型消融、动态阈值、决策追溯、州级风险、季度趋势。
- 至少准备 8 个预设问题，其中 4 个直接围绕 M1 核心结论。
- 每个核心问题都返回 `answer`，适合可视化时返回 `chart_title` / `chart_note` / `chart`。
- 生成代码可折叠展示，且能说明图表含义和指标口径。
- 现场 demo 至少完成 3 次连续问答：模型增益、策略收益、决策追溯。
- `.env`、临时图表、运行时缓存不进入 Git。

---

## 六、M3 Dashboard 核心展示系统

Dashboard 不再追求 9 Tab，而是围绕 M1 形成 4 个核心展示区 + 1 个 AI 助手区。

| 展示区 | 重点问题 | 内容 | 优先级 |
|---|---|---|---|
| 状态风险 | 哪些宏观状态风险更高？ | 状态分布、状态违约率、季度趋势、州级差异 | P0 |
| 模型验证 | 状态感知特征有没有提升模型？ | 消融实验、AUC/KS/PR-AUC、Top 特征、Calibration | P0 |
| 动态策略 | 动态阈值是否优于固定阈值？ | 阈值-利润曲线、状态阈值表、通过率/坏账率对比 | P0 |
| 决策追溯 | 单笔贷款为什么通过/拒绝？ | 样本选择、评分、阈值、解释、反事实改善路径 | P1 |
| AI 助手 | 能否自然语言查询核心结论？ | 已完成的 M2，接入 M1 数据源 | P1 |

M3 验收标准：

- 每个核心展示区至少有 1 张动态图或 1 个筛选器。
- 产物缺失时友好提示，不阻塞 Dashboard 启动。
- 现场 demo 能围绕 M1 连续讲 8-12 分钟。

---

## 七、M4 报告 / Demo / 答辩收口

### 7.1 报告结构

报告不按代码模块写，按研究逻辑写：

| 章节 | 核心内容 | 负责人 |
|---|---|---|
| 研究背景 | 个人贷款违约风险、宏观非平稳环境、固定阈值局限 | xwp |
| 数据与融合 | Lending Club + FRED + ERS，多源对齐和特征说明 | xwp |
| 状态感知动态风控框架 | 状态识别、状态特征、动态阈值、追溯链路 | xwp + zyf |
| 建模与实验验证 | baseline vs 状态感知模型，消融实验，AutoML 验证 | xwp |
| 因果 / 反事实 / 决策追溯 | 个体解释和可审计风控案例 | zyf 主，xwp 协 |
| 智能分析与可视化系统 | Dashboard + LLM 助手如何展示核心结论 | xwp |
| 总结与局限 | 收益、限制、未来改进 | 共同 |

### 7.2 Demo 路径

推荐 10-12 分钟演示路径：

1. 数据概览：说明多源融合和 baseline。
2. 状态风险：展示宏观状态下违约率差异。
3. 模型验证：展示状态感知模型相对 baseline 的 AUC/KS/业务指标变化。
4. 动态策略：拖动或选择状态，看最佳阈值、利润、坏账率变化。
5. 决策追溯：展示一笔样本从评分到审批结果的链路。
6. AI 助手：用自然语言问“动态阈值相比固定阈值提升多少？”并自动出图。

### 7.3 最终交付检查

- 一键运行脚本或分步运行说明完整。
- M1 关键产物路径明确。
- Dashboard 启动无报错。
- LLM 依赖 `.env`，无 key 泄露。
- 备用截图准备齐全，防止现场网络或 LLM 不稳定。

---

## 八、实施节奏

| # | 任务 | 负责人 | 产物 | 验收 |
|---|---|---|---|---|
| 1 | M1-1 宏观状态构造 | zyf 主，xwp 协 | 状态标签表、状态分布图、状态违约率对比 | 状态能解释违约率差异 |
| 2 | M1-2 状态感知特征工程 | zyf + xwp | 特征清单、缺失率表、建模数据构造逻辑 | 与 baseline 兼容，不污染默认特征空间 |
| 3 | M1-3 状态感知建模验证 | xwp | 消融模型指标表、AUC/KS 图、Top 特征 | 有明确量化结论 |
| 4 | M1-4 动态阈值策略优化 | xwp 主，zyf 协 | 策略收益表、最佳阈值表、利润曲线 | 动态策略优劣有业务解释 |
| 5 | M1-5 反事实与决策追溯 | zyf 主，xwp 协 | 典型案例表、追溯图 | 至少 3 个案例可讲清 |
| 6 | M2 LLM 助手接 M1 产物 | xwp | M1 数据源、预设问题、自动报告摘要 | 能问 M1 核心问题并出图 |
| 7 | M3 Dashboard 核心展示 | xwp + zyf | 状态风险、模型验证、动态策略、追溯展示 | 核心结果可交互查看 |
| 8 | M4 Demo 与报告收口 | xwp + zyf | 演示脚本、报告、备用截图 | 计时演练通过 |

---

## 九、协作机制

### 9.1 分工边界

| 维度 | xwp | zyf |
|---|---|---|
| 数据融合 | 多源数据读取、路径常量、建模数据接口 | 时序/状态变量定义建议 |
| 核心方法 | 状态感知建模验证、动态阈值收益评估 | 宏观状态、时序、因果、反事实方法 |
| 模型实验 | LR / XGBoost / LightGBM / AutoML / 消融实验 | 提供状态和时序特征语义 |
| 策略优化 | 固定阈值 vs 状态阈值收益对比 | 动态阈值规则合理性 |
| 解释追溯 | SHAP、个案展示、Dashboard 接入 | 反事实、因果解释、追溯语义 |
| 系统展示 | Dashboard、LLM 助手、demo 路径 | 专题结果解读和案例素材 |

### 9.2 工程铁律

1. **公共加载函数保持向后兼容**：新增状态/时序特征必须用开关控制，默认 baseline 不变。
2. **业务常量集中管理**：阈值、特征组、路径、输出文件名统一放在 `constant/`，不写魔法值。
3. **产物优先小而稳定**：每个 M1 子任务先输出小表和图，再考虑复杂模型。
4. **每次改签名同步调用点**：训练、策略、Dashboard、LLM 数据源都要闭环检查。
5. **提交前检查运行时产物**：不提交 `.env`、临时图表、模型大文件和原始数据。

### 9.3 Git 模型

- main 做交付分支。
- 每个模块以可运行产物为提交单位。
- PR / commit 描述必须写清：改了哪些产物、跑了哪些验证、影响 Dashboard/LLM 哪些入口。

---

## 十、汇报分工与时间分配

### 10.1 汇报分工

| 章节 | 谁讲 | 重点 |
|---|---|---|
| 研究背景与问题 | xwp | 为什么宏观非平稳环境下固定模型/固定阈值不够 |
| 数据与多源融合 | xwp | Lending Club + FRED + ERS 如何对齐 |
| 状态感知动态风控框架 | xwp + zyf | M1 主线和创新点 |
| 模型与策略验证 | xwp | 消融实验、AutoML、动态阈值收益 |
| 因果 / 反事实 / 追溯 | zyf | 方法学和案例解释 |
| Dashboard + LLM Demo | xwp | 交互展示和自然语言自动出图 |
| 总结与 Q&A | 共同 | 贡献、局限、下一步 |

### 10.2 演讲时间分配（30 min）

| 时段 | 内容 | 谁讲 |
|---|---|---|
| 3 min | 研究背景与问题 | xwp |
| 5 min | 数据融合与 baseline | xwp |
| 8 min | M1 状态感知动态风控框架 | xwp + zyf |
| 8 min | 现场 demo：状态风险 → 模型验证 → 动态策略 → 追溯 → LLM 问答 | xwp |
| 4 min | 因果/反事实方法学与案例 | zyf |
| 2 min | 总结 + Q&A | 共同 |

最终表达口径：

> 我们不是单纯提升 AUC，而是将个人贷款违约预测扩展为一个面向宏观非平稳环境的动态风险决策系统，使模型能够感知状态、验证增益、调整阈值、解释个案，并通过 Dashboard 与 LLM 助手完成交互式展示。
